var answer - pickRandom([
    'certainly',
    'ask again',
    'reply',
    'doubtful',
    'outlook good'
]);
print(answer);


//para criar um quadrado quadriculado

var colors = [
    'white',
    'black'
];
drawBox(colors[1]);
drawBox(colors[0]);
drawBox(colors[1]);
newLine();
drawBox(colors[1]);
drawBox(colors[0]);
drawBox(colors[1]);
newLine();
drawBox(colors[0]);
drawBox(colors[1]);
drawBox(colors[0]);